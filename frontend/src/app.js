import {Router} from "./router.js";

class App {

    constructor() {   // в этом конструкторе будем запускать какое-то действие, которое будет запускать процесс на любой странице

            new Router();
    }

}

(new App());